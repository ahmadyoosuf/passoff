# PASSOFF Configuration
# Mode: "mock" (default) uses pre-committed scenario outputs
#       "live" calls Claude API for real-time scoring
PASSOFF_MODE=mock

# Required only when PASSOFF_MODE=live
# ANTHROPIC_API_KEY=your-key-here
